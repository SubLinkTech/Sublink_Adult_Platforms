# SubLink DataTypes Joystick Events

[Back To Readme](../../../README.md)  
[Back To Joystick DataTypes Index](Index.md)

## ReactToChatMessage

- Parameter type `JoystickChatMessageEventArgs`
- Boilerplate
```csharp
joystick.ReactToChatMessage(async (chatMessage) => {
    // Your Code
});
```

## ReactToBotMessage

- Parameter type `JoystickBotMessageEventArgs`
- Boilerplate
```csharp
joystick.ReactToBotMessage(async (botMessage) => {
    // Your Code
});
```

## ReactToEnterStream

- Parameter type `JoystickEnterStreamEventArgs`
- Boilerplate
```csharp
joystick.ReactToEnterStream(async (event) => {
    // Your Code
});
```

## ReactToLeaveStream

- Parameter type `JoystickLeaveStreamEventArgs`
- Boilerplate
```csharp
joystick.ReactToLeaveStream(async (event) => {
    // Your Code
});
```

## ReactToStarted

- Parameter type `JoystickWhoEventArgs`
- Boilerplate
```csharp
joystick.ReactToStarted(async (event) => {
    // Your Code
});
```

## ReactToStreamResuming

- Parameter type `JoystickWhoEventArgs`
- Boilerplate
```csharp
joystick.ReactToStreamResuming(async (event) => {
    // Your Code
});
```

## ReactToStreamEnding

- Parameter type `JoystickWhoEventArgs`
- Boilerplate
```csharp
joystick.ReactToStreamEnding(async (event) => {
    // Your Code
});
```

## ReactToEnded

- Parameter type `JoystickWhoEventArgs`
- Boilerplate
```csharp
joystick.ReactToEnded(async (event) => {
    // Your Code
});
```

## ReactToFollowed

- Parameter type `JoystickWhoWhatEventArgs`
- Boilerplate
```csharp
joystick.ReactToFollowed(async (event) => {
    // Your Code
});
```

## ReactToFollowerCountUpdated

- Parameter type `JoystickFollowerCountUpdatedEventArgs`
- Boilerplate
```csharp
joystick.ReactToFollowerCountUpdated(async (event) => {
    // Your Code
});
```

## ReactToTipped

- Parameter type `JoystickTippedEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipped(async (event) => {
    // Your Code
});
```

## ReactToTipGoalCreated

- Parameter type `JoystickTitleAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipGoalCreated(async (event) => {
    // Your Code
});
```

## ReactToTipGoalDeleted

- Parameter type `JoystickTitleAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipGoalDeleted(async (event) => {
    // Your Code
});
```

## ReactToTipGoalIncreased

- Parameter type `JoystickTipGoalIncreasedEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipGoalIncreased(async (event) => {
    // Your Code
});
```

## ReactToTipGoalMet

- Parameter type `JoystickWhoWhatTitleAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipGoalMet(async (event) => {
    // Your Code
});
```

## ReactToTipGoalUpdated

- Parameter type `JoystickTitleAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipGoalUpdated(async (event) => {
    // Your Code
});
```

## ReactToTipMenuItemLocked

- Parameter type `JoystickTitleAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipMenuItemLocked(async (event) => {
    // Your Code
});
```

## ReactToTipMenuItemUnlocked

- Parameter type `JoystickTitleAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToTipMenuItemUnlocked(async (event) => {
    // Your Code
});
```

## ReactToChatTimerStarted

- Parameter type `JoystickChatTimerStartedEventArgs`
- Boilerplate
```csharp
joystick.ReactToChatTimerStarted(async (event) => {
    // Your Code
});
```

## ReactToChatTimersCleared

- Parameter type `JoystickWhoEventArgs`
- Boilerplate
```csharp
joystick.ReactToChatTimersCleared(async (event) => {
    // Your Code
});
```

## ReactToDropinStream

- Parameter type `JoystickDropinStreamEventArgs`
- Boilerplate
```csharp
joystick.ReactToDropinStream(async (event) => {
    // Your Code
});
```

## ReactToStreamDroppedIn

- Parameter type `JoystickStreamDroppedInEventArgs`
- Boilerplate
```csharp
joystick.ReactToStreamDroppedIn(async (event) => {
    // Your Code
});
```

## ReactToSubscribed

- Parameter type `JoystickWhoWhatAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToSubscribed(async (event) => {
    // Your Code
});
```

## ReactToResubscribed

- Parameter type `JoystickResubscribedEventArgs`
- Boilerplate
```csharp
joystick.ReactToResubscribed(async (event) => {
    // Your Code
});
```

## ReactToGiftedSubscriptions

- Parameter type `JoystickWhoWhatAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToGiftedSubscriptions(async (event) => {
    // Your Code
});
```

## ReactToWheelSpinClaimed

- Parameter type `JoystickWheelSpinClaimedEventArgs`
- Boilerplate
```csharp
joystick.ReactToWheelSpinClaimed(async (event) => {
    // Your Code
});
```

## ReactToViewerCountUpdated

- Parameter type `JoystickAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToViewerCountUpdated(async (event) => {
    // Your Code
});
```

## ReactToSubscriberCountUpdated

- Parameter type `JoystickAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToSubscriberCountUpdated(async (event) => {
    // Your Code
});
```

## ReactToMilestoneCompleted

- Parameter type `JoystickWhoWhatTitleAmountEventArgs`
- Boilerplate
```csharp
joystick.ReactToMilestoneCompleted(async (event) => {
    // Your Code
});
```

## ReactToPvpSessionRequested

- Parameter type `JoystickPvpSessionRequestedEventArgs`
- Boilerplate
```csharp
joystick.ReactToPvpSessionRequested(async (event) => {
    // Your Code
});
```

## ReactToPvpSessionReady

- Parameter type `JoystickPvpSessionReadyEventArgs`
- Boilerplate
```csharp
joystick.ReactToPvpSessionReady(async (event) => {
    // Your Code
});
```

## ReactToPvpSessionStarted

- Parameter type `JoystickPvpSessionStartedEventArgs`
- Boilerplate
```csharp
joystick.ReactToPvpSessionStarted(async (event) => {
    // Your Code
});
```

## ReactToPvpSessionEnding

- Parameter type `JoystickWhoWhatWhenWhereEventArgs`
- Boilerplate
```csharp
joystick.ReactToPvpSessionEnding(async (event) => {
    // Your Code
});
```

## ReactToPvpSessionEnded

- Parameter type `JoystickWhoWhatWhenWhereEventArgs`
- Boilerplate
```csharp
joystick.ReactToPvpSessionEnded(async (event) => {
    // Your Code
});
```

## ReactToSceneUpdated

- Parameter type `JoystickSceneUpdatedEventArgs`
- Boilerplate
```csharp
joystick.ReactToSceneUpdated(async (event) => {
    // Your Code
});
```

## ReactToSettingsUpdated

- Parameter type `JoystickEventArgs`
- Boilerplate
```csharp
joystick.ReactToSettingsUpdated(async (event) => {
    // Your Code
});
```

## ReactToStreamModeUpdated

- Parameter type `JoystickWhoEventArgs`
- Boilerplate
```csharp
joystick.ReactToStreamModeUpdated(async (event) => {
    // Your Code
});
```

## ReactToUserMuted

- Parameter type `JoystickWhoWhatEventArgs`
- Boilerplate
```csharp
joystick.ReactToUserMuted(async (event) => {
    // Your Code
});
```

## ReactToUserUnmuted

- Parameter type `JoystickWhoWhatEventArgs`
- Boilerplate
```csharp
joystick.ReactToUserUnmuted(async (event) => {
    // Your Code
});
```

## ReactToDeviceConnected

- Parameter type `JoystickEventArgs`
- Boilerplate
```csharp
joystick.ReactToDeviceConnected(async (event) => {
    // Your Code
});
```

## ReactToDeviceDisconnected

- Parameter type `JoystickEventArgs`
- Boilerplate
```csharp
joystick.ReactToDeviceDisconnected(async (event) => {
    // Your Code
});
```

## ReactToDeviceSettingsUpdated

- Parameter type `JoystickEventArgs`
- Boilerplate
```csharp
joystick.ReactToDeviceSettingsUpdated(async (event) => {
    // Your Code
});
```
